<%= render 'layouts/stylesheets' %>
</br></br>

<table class='pagetext'>
	<% @current_events.each do |current_event| %>
		<tr>
			<td class="pagetitle"><%=raw current_event.title %></td>
		</tr>
		<tr></tr><tr></tr>
		<tr>
			<td class="pagetext"><%=raw current_event.page_text %></td>
		</tr>
		<% end %>
</table>

<%= link_to 'New Current event', new_current_event_path %>
