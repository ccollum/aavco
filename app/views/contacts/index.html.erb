<%= render 'layouts/stylesheets' %>
</br></br>

<table class='pagetext'>
	<% @contacts.each do |contact| %>
		<tr>
			<td class='pagetitle'><%=raw contact.title %></td>
		</tr>
		<tr></tr><tr></tr>
			<td class='pagetext'><%=raw contact.page_text %></td>
		</tr>
	<% end %>
</table>
