<%= form_for(@service) do |f| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="pagetitle" width=400px>
    <%= f.text_field :title %>
  </div>
  <div class="pagetext" width=100%>
<br />
    <%= f.text_area :page_text, :class=>"tinymce", :rows=>100, :cols=>300 %>
    <%= tinymce %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
