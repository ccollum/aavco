<%= form_for(@about_aa) do |f| %>
  <% if @about_aa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about_aa.errors.count, "error") %> prohibited this about_aa from being saved:</h2>

      <ul>
      <% @about_aa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :page_text %><br />
    <%= f.text_area :page_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
